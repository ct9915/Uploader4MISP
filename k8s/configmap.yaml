apiVersion: v1
kind: ConfigMap
metadata:
  name: uploader4misp-config
  namespace: default
  labels:
    app: uploader4misp
data:
  FLASK_ENV: "production"
  PORT: "5000"
  WEB_CONCURRENCY: "4"
  MISP_VERIFYCERT: "false"
  MAIL_PORT: "587"
  MAIL_USE_TLS: "true"
